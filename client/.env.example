# Copy to .env.local for local override (optional).
# On Vercel set the same key in Project Settings → Environment Variables (Production + Preview).
VITE_API_BASE_URL=https://flor-articular-proficiently.ngrok-free.dev
